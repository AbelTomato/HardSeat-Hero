# 开发日志

## 2026-06-25 初始化构建与范围收敛

### 当前目标

项目目标已收敛为：根据出发地、目的地和出行日期，查询最低价火车直达或一次中转方案列表。

系统不提供“最短耗时”或“最少中转”排序能力。总耗时、换乘站和换乘等待时间只作为方案辅助信息展示。

### 已完成成果

#### 文档

- 新增 `docs/需求分析.md`，明确 MVP 范围、非功能需求和数据风险。
- 新增 `docs/总体设计.md`，记录前后端架构、API、领域模型、数据源适配方式和算法概要。
- 新增 `docs/实施计划.md`，拆分项目骨架、领域模型、搜索算法、前端体验、真实数据源接入和交付验证阶段。
- 新增 `docs/数据源调研.md`，记录 GitHub 开源项目、MCP 查询项目、Mock 数据源和缓存策略的调研方向。
- 新增本文件 `docs/开发日志.md`，记录当前阶段开发结果。

#### 后端

- 初始化 `backend/` FastAPI 项目。
- 新增 `backend/app/main.py`，配置 FastAPI 应用和 CORS。
- 新增 `backend/app/api/routes.py`，提供以下接口：
  - `GET /api/health`
  - `GET /api/stations/search`
  - `GET /api/providers/status`
  - `POST /api/routes/search`
- 新增 `backend/app/domain/models.py`，定义：
  - `SeatPrice`
  - `TrainSegment`
  - `RouteQuery`
  - `TransferPlan`
  - `RouteSearchResponse`
  - `StationSearchResponse`
- 新增 `backend/app/adapters/base.py`，定义统一数据源接口 `TrainDataProvider`。
- 新增 `backend/app/adapters/mock_provider.py`，提供北京、上海、南京南、济南西、天津南、杭州东等 Mock 站点和车次数据。
- 新增 `backend/app/services/route_search.py`，实现直达和一次中转方案搜索。
- 搜索结果固定按最低总价排序；同价时用总耗时和换乘次数作为辅助排序。

#### 前端

- 初始化 `frontend/` React + Vite + TypeScript 项目。
- 使用 pnpm 管理前端依赖。
- 配置 Tailwind CSS v4 和 Vite 插件。
- 新增 shadcn/ui 风格的本地基础组件：
  - `frontend/src/components/ui/button.tsx`
  - `frontend/src/components/ui/input.tsx`
- 新增 `frontend/src/lib/api.ts`，封装 `/api/routes/search` 请求。
- 新增 `frontend/src/App.tsx`，实现：
  - 出发地输入。
  - 目的地输入。
  - 日期输入。
  - 最短换乘时间输入。
  - 查询提交。
  - 最低价方案列表展示。
  - 空状态和错误状态。
- 已移除“最短耗时”和“最少中转”排序切换，避免偏离项目目标。

#### 项目配置

- 新增 `README.md`，记录项目介绍、启动方式、测试方式和本地联调方式。
- 新增 `.gitignore`，忽略 Python 虚拟环境、缓存、egg-info、node_modules、dist、环境变量文件等生成物。
- 生成 `frontend/pnpm-lock.yaml`。

### 当前验证结果

#### 后端测试

命令：

```powershell
backend\.venv\Scripts\python.exe -m pytest backend
```

结果：

- `5 passed`
- `1 warning`

警告来源：FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

#### 前端构建

命令：

```powershell
pnpm --dir frontend build
```

结果：构建通过。

#### 接口联调

后端服务已启动：

```powershell
backend\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000 --app-dir backend
```

前端服务已启动：

```powershell
pnpm --dir frontend dev
```

当前访问地址：

- 前端：`http://localhost:5173/`
- 后端：`http://127.0.0.1:8000/api`

已验证接口：

- `GET /api/health` 返回正常。
- `POST /api/routes/search` 可返回北京到上海最低价方案。

当前 Mock 数据中，北京到上海最低价方案为：

- 路线：北京 -> 南京南 -> 上海
- 中转站：南京南
- 总价：`413.0`
- 总耗时：`360` 分钟
- 换乘等待：`45` 分钟

### 当前重要约束

- 项目只查询最低价中转方案，不做最短耗时或最少中转排序。
- 官方实时数据暂不作为第一阶段强依赖。
- GitHub 开源项目或 MCP 查询能力后续必须通过 `TrainDataProvider` 适配，不应直接耦合到搜索算法。
- 当前数据为 Mock 数据，只用于算法和 UI 闭环验证。

### 下一步建议

1. 增加数据源缓存层，为真实数据源接入做准备。
2. 调研 GitHub/MCP 可用数据源，记录许可证、维护状态、调用方式和失效风险。
3. 实现第一个真实数据源适配器，并保持 Mock 数据源可切换。
4. 增加前端接口错误细分展示，例如后端不可用、数据源不可用、无方案。

## 2026-06-25 站点自动补全

### 已完成成果

- 前端新增站点查询封装 `searchStations`，调用 `GET /api/stations/search`。
- 出发地和目的地输入框接入独立 `datalist`，根据各自当前输入展示候选站点。
- 后端 API 测试补充 `GET /api/stations/search` 覆盖。
- README 已补充站点自动补全说明。

### 当前验证计划

- 后端测试继续覆盖站点搜索和路线查询接口。
- 前端构建验证 TypeScript、Vite 和 Tailwind 配置正常。

## 2026-06-25 后端查询缓存层

### 已完成成果

- 新增 `TtlCache`，提供简单内存 TTL 缓存能力。
- `RouteSearchService` 现在缓存 `TrainDataProvider.search_segments` 的单段车次查询结果。
- 缓存 key 使用 `provider:date:from_station:to_station` 结构，便于后续真实数据源接入。
- 新增缓存单元测试，覆盖命中、过期和非法 TTL。
- 新增搜索服务缓存测试，确保重复查询不会重复调用 Provider。

### 当前约束

- 当前缓存为进程内内存缓存，服务重启后失效。
- 后续接入真实数据源后，可替换或扩展为 Redis，但不应改变 `RouteSearchService` 对 Provider 的调用边界。

## 2026-06-25 票价获取方案调研

### 已完成成果

- 确认 `Parse12306` 可提供车站、车次和经停时刻表抓取思路，但不提供票价。
- 确认 `China-rail-way-stations-data` 只解析车站和车次始发终到数据，且 README 标明数据过期，不提供票价。
- 评估用户提供的 `leftTicket/queryZ` 与 `queryTicketPrice` 示例代码：链路可行，但代码存在交互式输入、全局状态、席别分支错误和错误处理不足，不能直接接入服务端。
- 新增 `docs/票价获取方案.md`，记录 12306 余票和票价接口链路、字段映射、缓存策略和失败降级。
- 更新 `docs/数据源调研.md` 和 `docs/实施计划.md`，把票价接口探测纳入真实数据源接入阶段。
- 新增 `backend/scripts/probe_12306_pricing.py`，用于手动探测站点电报码、余票和票价接口。
- 后续验证发现旧 `leftTicket/queryZ`、`leftTicket/query`、`leftTicket/queryA` 当前返回 HTML，不适合作为主路径。
- 已确认 12306 官方公布票价页面 `queryPublicIndex.html` 使用 `leftTicketPrice/queryAllPublicPrice`，该接口可直接返回车次、时刻和公布票价 JSON。
- 探测脚本已改为使用 `leftTicketPrice/queryAllPublicPrice`，并支持高铁动车与 K/Z 普速车价格字段解析。

### 当前约束

- 12306 相关接口不是公开稳定 API，字段和 header 要求可能变化。
- 探测脚本不接入主 API，不影响 Mock 数据源和核心搜索算法。
- 正式适配器仍需补充 fixture、单元测试、缓存、`infoAll_list` 扩展解析和配置化 provider 切换。

## 2026-06-25 12306 公布票价适配器最小实现

### 已完成成果

- 新增 `Railway12306PublicPriceProvider`，通过 `TrainDataProvider` 边界接入 12306 公布票价接口。
- 新增 `StationCodeRepository`，下载并缓存站名到电报码映射。
- 新增 `PublicPriceClient`，请求 `leftTicketPrice/queryAllPublicPrice` 并对非 JSON、失败响应、网络错误做受控错误包装。
- 新增 `provider_factory`，支持通过环境变量 `TRAIN_DATA_PROVIDER=mock|12306-public-price` 切换数据源，默认仍为 `mock`。
- API 层捕获 `Railway12306Error` 并返回 `502`，避免真实数据源异常影响服务进程。
- 新增 12306 解析单元测试，覆盖站点解析、高铁票价字段、普速票价字段、跨日、缺失价格、provider 工厂。

### 当前验证结果

命令：

```powershell
backend\.venv\Scripts\python.exe -m pytest backend
```

结果：

- `25 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- 12306 provider 第一版只支持直达 OD 查询；`candidate_transfer_stations` 暂不枚举真实全量中转站，避免无约束放大外部请求量。
- 第一版只解析明确 `*_price` 字段，暂不解析 `infoAll_list`。
- 真实数据源依赖外部网络和 12306 非公开稳定接口，不应作为默认 provider。

## 2026-06-25 前端错误细分展示

### 已完成成果

- 前端 API 封装新增 `ApiError`，按 HTTP 状态区分请求参数错误、数据源不可用和未知错误。
- 查询页面现在会将 `502/503/504` 展示为“数据源不可用”，将 `422` 展示为“查询参数错误”。
- 网络连接失败时展示“后端服务不可用”，避免只显示浏览器原始异常。

## 2026-06-26 中转搜索算法与真实数据源稳定性优化

### 已完成成果

#### 设计文档

- 新增 `docs/中转搜索算法优化路线.md`，记录从当前中转搜索到 Best First Search / A* 的完整演进路线。
- 明确项目长期定位为“全国铁路最低票价路径搜索器”，搜索主目标保持为最低总票价。
- 明确历史最低票价通常是上界，不应直接作为 A* admissible lower bound 用于硬剪枝。

#### 一换搜索优化

- `RouteSearchService` 一换搜索增加第一段为空剪枝：`A -> T` 无车次时不再查询 `T -> B`。
- 换乘组合从双层暴力枚举调整为按第二段发车时间排序并用二分定位可换乘车次。
- 中转方案补充 `max_total_duration_minutes` 过滤，避免跨日超长等待方案误入结果。
- 搜索结果仍按 `total_price` 优先排序，总耗时和换乘次数只作为同价稳定排序条件。

#### 受控并发与请求预算

- `RouteSearchService` 新增 `max_concurrent_remote_queries`，使用 `asyncio.Semaphore` 控制真实远程查询并发。
- 使用 `asyncio.Lock` 保护 `remote_query_count` 检查和递增，避免并发下超出 `max_remote_queries`。
- `/api/providers/status` 增加 `max_concurrent_remote_queries` 诊断字段。
- 前端状态面板同步展示 OD 请求预算、并发上限和上次实际请求数。

#### 候选站低价评分

- `CandidateTransferStationGenerator` 新增 `CHEAP_TRANSFER_SCORES` 静态低价断点评分。
- 候选站排序从几何距离优先调整为：低价断点评分、铁路中心性、走廊垂距、站名。
- 保留地理走廊过滤，避免候选站无约束扩散。

#### 12306 错误诊断与候选容错

- 新增通用 `TrainDataProviderError`，`Railway12306Error` 继承该错误类型。
- 新增 `describe_httpx_error()`，让 12306 请求失败时包含异常类型、HTTP 状态、请求 URL 或超时说明，避免只显示空的“票价接口请求失败：”。
- 一换候选站单个 OD 请求失败时跳过该候选，不再中断整次搜索。
- 使用探测脚本确认 `leftTicketPrice/queryAllPublicPrice` 当前可访问，但部分 OD 可能返回 0 车次或偶发失败。

#### `best_price` 剪枝

- 新增 `BestPriceTracker`，在并发候选任务间共享当前已知最低价。
- 直达方案会先更新 `best_price`，后续中转搜索可基于该价格剪枝。
- 当第一段票价已不可能优于当前最低价时，不再查询或组合后续方案。
- 当完整中转方案总价不低于当前最低价时跳过该方案。

#### A* / Uniform Cost Search 搜索器

- 新增 `PathSearchState`，表示 A* 状态：当前站、已选车次、累计票价、到达时间和已访问站点。
- 新增 `CheapestPathSearcher`，使用优先队列按 `g + h` 扩展路径。
- 当前 `_heuristic_lower_bound()` 返回 `0`，因此实现等价于 Uniform Cost Search，优先保证最低价正确性。
- 当 `RouteQuery.max_transfers >= 2` 时启用新搜索器，支持两次中转搜索。
- 保留 `max_transfers = 0/1` 的原优化路径，降低回归风险。

#### Pareto frontier 支配剪枝

- 新增 `FrontierEntry` 和 `ParetoFrontier`。
- 状态只有在已有状态“累计价格不高且到达时间不晚”时才会被剪枝。
- 保留“更贵但更早到达”的状态，避免因忽略时间维度误剪可行低价后续路径。

#### OD SQLite 持久化缓存

- `RouteSearchService` 新增可选 `persistent_cache`。
- 支持通过环境变量启用默认 SQLite OD 缓存：
  - `ROUTE_SEGMENT_CACHE_DB`
  - `ROUTE_SEGMENT_CACHE_TTL_SECONDS`
- `_search_segments()` 查询顺序调整为：内存 TTL 缓存 -> SQLite OD 缓存 -> 远程 Provider。
- SQLite 命中会回填内存缓存，并且不增加 `remote_query_count`。
- 新增 `serialize_train_segments()` 和 `deserialize_train_segments()`，使用 Pydantic JSON 模式序列化 `TrainSegment`。

#### 搜索遥测与历史命中记录

- 新增 `SqliteSearchTelemetryRecorder`，持久化记录每次搜索：
  - 查询时间。
  - provider。
  - 出发地、目的地、出行日期。
  - 最大中转次数、最短换乘时间、最大总时长。
  - 方案数量、最佳价格、最佳中转站、远程请求数。
- 新增 `transfer_station_hits` 表，累计某 OD 下中转站命中次数和历史最佳价格。
- `RouteSearchService` 新增可选 `telemetry_recorder`。
- 支持通过环境变量启用默认 SQLite 遥测库：`SEARCH_TELEMETRY_DB`。
- `search()` 和 `stream()` 完成后都会写入搜索遥测。

### 当前验证结果

#### 后端测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `54 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

#### 前端构建

命令：

```powershell
pnpm --dir frontend build
```

结果：构建通过。

### 当前重要约束

- 当前 A* 的启发函数仍为 `h=0`，等价 Uniform Cost Search，正确性优先但剪枝能力有限。
- 历史最低票价通常是上界，不是安全下界，不能直接用于 A* 硬剪枝。
- 搜索遥测当前只作为后续候选排序和学习闭环的数据基础，尚未反哺实时候选排序。
- SQLite 缓存和遥测默认不启用，必须通过环境变量显式配置路径。

### 下一步建议

1. 用 `transfer_station_hits` 反哺候选站排序：历史命中次数高、历史最佳价格低的中转站优先扩展。
2. 保持 A* `h=0`，先只改变候选扩展顺序，不改变硬剪枝规则。
3. 后续若要实现真正启发函数，必须先设计不会高估的 lower bound 来源。
4. 增加搜索遥测查询接口或管理脚本，便于观察不同 OD 的候选站命中效果。

## 2026-06-26 最低价结果体验排序与后续瓶颈判断

### 用户体会

- 当前目标不是泛化推荐路线，而是严格围绕“最低总价”筛选结果。
- 同价方案不应简单按总耗时排序；中途等待时间会明显影响体验，应作为同价排序依据。
- 返回结果不需要过多，保留 `10` 个方案足够用于前端展示和用户比较。
- 真正的性能瓶颈大概率不在本地搜索算法，而在真实数据源远程 I/O。
- 后续更重要的方向是实现数据库持久化，记录站点或 OD 历史最低价及更新时间。铁路公布票价相对稳定，可以较长时间不更新，用于减少重复外部请求。

## 2026-06-27 静态票价库诊断与遥测导出计划落地

### 已完成成果

- 新增静态票价库诊断脚本 `backend/scripts/inspect_static_prices.py`。
- 新增搜索遥测导出刷新 OD 脚本 `backend/scripts/export_refresh_od_from_telemetry.py`。
- 刷新 OD CSV 使用 `origin,destination,priority,reason` 列。
- `refresh_static_prices.py` 可继续读取 `origin,destination` 并忽略额外列。
- 新增对应测试，覆盖空库、指定 OD、遥测高频查询、中转命中展开、CSV 写出。

### 验证结果

```powershell
cd backend
python -m pytest -q
```

结果：待执行后回填。

### 已完成成果

#### 结果数量收敛

- `RouteSearchService` 和 `CheapestPathSearcher` 的返回上限统一收敛为 `MAX_RETURNED_PLANS = 10`。
- `search()`、`stream()` 和多次中转搜索结果截断均使用同一常量，避免魔法数字分散。

#### 同价舒适性排序

- 搜索主排序仍保持严格最低价优先。
- 新增 `_plan_sort_key()`，排序顺序调整为：
  1. `total_price`
  2. 中途等待时间舒适性惩罚
  3. 换乘次数
  4. 总耗时
- 新增 `_transfer_wait_comfort_penalty()`：
  - 换乘等待 `30~120` 分钟视为舒适，惩罚为 `0`。
  - 等待小于 `30` 分钟视为过短，按风险加重惩罚。
  - 等待超过 `120` 分钟视为过长，按超出分钟数惩罚。
- 该评分只影响同价方案排序，不改变合法性过滤，也不改变最低价主目标。

#### 同价方案保留

- 价格剪枝边界从 `>= best_price` 调整为 `> best_price`。
- 一换搜索和多次中转搜索都允许保留同价候选，避免同价舒适性排序没有候选可排。

#### 方案去重

- 新增 `_deduplicate_plans()` 和 `_plan_identity_key()`。
- 按车次号、起终站、出发时间和到达时间组成方案身份，去除重复方案。
- 该问题在测试同价换乘时暴露：同一车次组合可能因日期窗口查询重复进入结果。

### 修改文件

- `backend/app/services/route_search.py`
- `backend/tests/test_route_search.py`

### 新增测试

- `test_search_returns_at_most_ten_plans`
  - 验证最多返回 `10` 个方案。
- `test_equal_price_transfer_plans_are_sorted_by_wait_comfort`
  - 验证同价中转方案按中途等待舒适性排序。

### 当前验证结果

#### 路线搜索测试

命令：

```powershell
cd backend; python -m pytest tests/test_route_search.py -q
```

结果：

- `25 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `60 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 下一步判断

- 短期不应继续把主要精力放在微调本地搜索循环上。
- 应优先设计并实现历史最低价持久化数据库，用稳定价格数据减少真实 Provider 的远程 I/O。
- 历史最低价可优先用于：
  - 候选 OD 排序。
  - 候选中转站排序。
  - 判断哪些 OD 值得优先查。
  - 给用户展示“历史最低价更新时间”。
- 但历史最低价未必是严格 admissible lower bound，不能直接用于 A* 硬剪枝，除非能证明不会高估当前真实最低价。

## 2026-06-26 静态票价数据库方案

### 方案判断

- 下一阶段重点应从继续微调本地搜索循环，转向建立本地静态票价数据库。
- 真实数据源瓶颈主要在远程 I/O；定时采集公布票价并本地查询，比每次搜索实时调用 12306 更稳定。
- 采用“定时全量/准全量采集 + 静态本地查询 + 缺失远程补洞”的方向是可行的，但不应直接枚举全国所有 OD 暴力爬取。

### 已完成成果

- 新增 `docs/静态票价数据库设计.md`，明确：
  - 静态票价库的产品语义是公布票价/历史观测最低票价，不保证实时余票。
  - 第一阶段使用 SQLite，不立即引入 Redis 或 PostgreSQL。
  - 核心表包括 `station_snapshot`、`od_public_price_snapshot`、`od_min_price_snapshot`、`crawl_job`、`crawl_task`。
  - 采集策略采用热门 OD、用户查询 OD、候选中转 OD、线路/车次级准全量的分层路线。
  - 查询链路为内存缓存、SQLite OD 缓存、SQLite 静态票价库、可选远程 fallback。
  - 历史最低价只能用于排序、刷新优先级和展示，不得直接作为 A* 硬剪枝 lower bound。

### 推荐实施顺序

1. 实现 `SQLiteStaticPriceRepository`，负责初始化表、写入 OD 票价快照、查询 OD segments、查询最低价和判断过期。
2. 实现 `LocalStaticPriceProvider`，通过现有 `TrainDataProvider` 边界从 SQLite 返回 `TrainSegment`。
3. 扩展 provider factory，支持 `TRAIN_DATA_PROVIDER=static-price`。
4. 新增 `refresh_static_prices.py`，第一版只接受明确 OD 列表，不做全国全量。
5. repository、provider 和刷新脚本稳定后，再接入 Windows Task Scheduler、cron、APScheduler 独立 worker 或 GitHub Actions。

### 当前约束

- API 服务进程不应负责长周期爬虫任务。
- 静态票价库不能承诺实时有票。
- 静态公布票价和实时余票应保持独立 TTL 与独立数据语义。
- 本地静态库接入后，搜索结果仍必须走 `RouteSearchService`，不能绕过既有最低价搜索逻辑。

## 2026-06-26 静态票价数据库任务 1：SQLite Repository

### 已完成成果

- 新增 `backend/app/services/static_price_repository.py`。
- 新增 `SQLiteStaticPriceRepository`，负责静态票价库第一阶段的数据访问。
- 新增 `StaticOdMinPrice`，表示某 OD 的最低价聚合快照。
- Repository 初始化时会创建以下表和索引：
  - `station_snapshot`
  - `od_public_price_snapshot`
  - `od_min_price_snapshot`
  - `crawl_job`
  - `crawl_task`
  - `idx_station_snapshot_code`
  - `idx_od_price_query`
  - `idx_od_price_train`
  - `idx_od_min_price_origin_dest`
  - `idx_crawl_task_pick`
- `upsert_segments()` 支持把现有 `TrainSegment` 写入 `od_public_price_snapshot`。
- 同一车次、同一席别重复写入时会覆盖更新价格、时刻、来源和 `fetched_at`。
- 写入 OD 票价后会刷新 `od_min_price_snapshot`，按最低价格、发车时间、车次和席别稳定选择最低价记录。
- `query_segments()` 支持从拆行席别价格记录重建 `TrainSegment` 列表。
- `get_min_price()` 支持读取 OD 最低价快照。
- `is_stale()`、`query_segments(max_age=...)` 和 `get_min_price(max_age=...)` 支持按数据年龄过滤过期数据。

### 新增测试

- 新增 `backend/tests/test_static_price_repository.py`，覆盖：
  - 空库返回空结果。
  - 写入并查询 OD 车次段。
  - 同一车次多个席别价格重建。
  - 同一车次席别价格覆盖更新。
  - 查询 OD 最低价聚合快照。
  - 过期数据过滤和 `is_stale()` 判断。

### 当前验证结果

#### 静态票价 Repository 测试

命令：

```powershell
cd backend; python -m pytest tests/test_static_price_repository.py -q
```

结果：

- `6 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `66 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- 本次只完成 SQLite repository，尚未接入 `TrainDataProvider`。
- `station_snapshot`、`crawl_job` 和 `crawl_task` 当前只建表，后续刷新脚本再写入。
- 下一步应实现 `LocalStaticPriceProvider`，通过现有数据源边界从 SQLite 返回 `TrainSegment`。

## 2026-06-26 静态票价数据库任务 2：LocalStaticPriceProvider

### 已完成成果

- 新增 `backend/app/adapters/static_price_provider.py`。
- 新增 `LocalStaticPriceProvider`，通过现有 `TrainDataProvider` 边界读取 SQLite 静态票价库。
- `LocalStaticPriceProvider.name` 固定为 `static-price`。
- `search_segments()` 保持异步 provider 接口，内部调用 `SQLiteStaticPriceRepository.query_segments()`。
- provider 查询固定使用 `static-price` 命名空间，避免误读其他 provider 写入的数据。
- 支持通过 `max_age` 过滤过期静态票价数据。
- `candidate_transfer_stations()` 复用 `CandidateTransferStationGenerator`，并支持测试或后续配置注入。
- 构造时必须提供 `repository` 或 `db_path`，避免静默创建无意义数据库路径。

### 新增测试

- 新增 `backend/tests/test_static_price_provider.py`，覆盖：
  - 从 repository 返回 `TrainSegment`。
  - provider 命名空间隔离。
  - 过期静态数据过滤。
  - 缺少 repository 和 db_path 时拒绝初始化。
  - 可通过 db_path 初始化 repository。
  - 候选中转站生成器委托。

### 当前验证结果

#### 静态票价 Provider 测试

命令：

```powershell
cd backend; python -m pytest tests/test_static_price_provider.py -q
```

结果：

- `6 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `72 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- 本次只完成静态 provider 类，尚未接入 provider factory。
- `TRAIN_DATA_PROVIDER=static-price` 当前仍不可用，需任务 3 扩展 `backend/app/adapters/provider_factory.py`。
- 远程 fallback 模式尚未实现；当前 provider 只读本地静态库。

## 2026-06-26 静态票价数据库任务 3：Provider Factory 接入

### 已完成成果

- 更新 `backend/app/adapters/provider_factory.py`。
- `create_train_data_provider()` 现在支持以下静态票价 provider 名称：
  - `static`
  - `static-price`
  - `local-static-price`
- 新增环境变量 `STATIC_PRICE_DB`，用于指定 SQLite 静态票价库路径。
- 新增环境变量 `STATIC_PRICE_MAX_AGE_DAYS`，用于配置静态票价数据最大可接受年龄。
- 当 `TRAIN_DATA_PROVIDER=static-price` 但未配置 `STATIC_PRICE_DB` 时，factory 会显式抛出 `ValueError`。
- 当 `STATIC_PRICE_MAX_AGE_DAYS <= 0` 时，factory 会显式抛出 `ValueError`。
- 默认 provider 仍为 `mock`，不影响现有本地开发和 API 测试。

### 可用环境变量

```powershell
TRAIN_DATA_PROVIDER=static-price
STATIC_PRICE_DB=data/static_prices.sqlite3
STATIC_PRICE_MAX_AGE_DAYS=30
```

### 新增测试

- 更新 `backend/tests/test_railway_12306_public_price.py` 中的 provider factory 测试，覆盖：
  - 默认仍使用 `mock`。
  - 继续支持 `12306-public-price`。
  - 支持 `TRAIN_DATA_PROVIDER=static-price`。
  - `static-price` 缺少 `STATIC_PRICE_DB` 时失败。
  - `STATIC_PRICE_MAX_AGE_DAYS=0` 时失败。

### 当前验证结果

#### Provider 相关测试

命令：

```powershell
cd backend; python -m pytest tests/test_railway_12306_public_price.py tests/test_static_price_provider.py -q
```

结果：

- `27 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `75 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- `static-price` provider 当前为本地只读模式。
- `STATIC_PRICE_MODE=static-with-remote-fallback` 尚未实现。
- 本地静态库需要先由测试、脚本或后续刷新任务写入数据，否则查询会返回空结果。
- 下一步应实现 `refresh_static_prices.py`，第一版只接受明确 OD 列表，不做全国全量。

## 2026-06-26 静态票价数据库任务 4：刷新脚本

### 已完成成果

- 新增 `backend/scripts/refresh_static_prices.py`。
- 刷新脚本第一版只接受明确 OD CSV，不做全国全量爬取。
- 支持读取 OD CSV，列名兼容：
  - `origin,destination`
  - `from_station,to_station`
  - `from,to`
- 支持通过 `12306-public-price` 作为刷新源 provider。
- 每个 OD 会构造 `RouteQuery(max_transfers=0)`，只刷新直达公布票价数据。
- 远程查询成功后写入 `SQLiteStaticPriceRepository`，静态库 provider 命名空间固定为 `static-price`。
- 刷新过程会写入 `crawl_job` 和 `crawl_task`：
  - 记录任务总数、成功数、失败数。
  - 单个 OD 失败时记录错误并继续后续 OD。
  - 支持 `partial_failed` 状态。
- 支持 `--interval-seconds` 控制 OD 请求间隔。
- 支持 `--limit` 小批量验证。

### 使用示例

OD CSV 示例：

```csv
origin,destination
北京,上海
北京,南京南
南京南,上海
```

刷新命令：

```powershell
cd backend
python scripts/refresh_static_prices.py --date 2026-07-01 --od-file ../data/seed_od.csv --db ../data/static_prices.sqlite3 --interval-seconds 1 --limit 10
```

刷新完成后可用静态 provider 查询：

```powershell
$env:TRAIN_DATA_PROVIDER="static-price"
$env:STATIC_PRICE_DB="data/static_prices.sqlite3"
$env:STATIC_PRICE_MAX_AGE_DAYS="30"
```

### 新增测试

- 新增 `backend/tests/test_refresh_static_prices.py`，覆盖：
  - OD CSV 读取。
  - 成功刷新写入静态票价表。
  - 成功刷新写入 `crawl_job` 和 `crawl_task`。
  - 单个 OD 失败时记录失败并继续。
  - `--limit` 对刷新数量生效。
  - 未知刷新源 provider 会失败。

### 当前验证结果

#### 刷新脚本测试

命令：

```powershell
cd backend; python -m pytest tests/test_refresh_static_prices.py -q
```

结果：

- `5 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `80 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- 刷新脚本当前只支持明确 OD 列表，不做自动生成全国 OD。
- 当前刷新源只支持 `12306-public-price`。
- 当前刷新直达 OD，不负责中转 OD 自动扩展；候选中转 OD 应由后续任务基于用户搜索、`transfer_station_hits` 或候选站生成器产生 CSV。
- 当前没有实现重试和 `next_retry_at` 调度；失败任务已记录，后续可单独读取失败 OD 再刷新。

## 2026-06-26 静态票价数据库任务 5：远程补洞写回

### 已完成成果

- 删除误生成的根目录 `$db` 文件。
- 新增 `backend/app/adapters/static_price_fallback_provider.py`。
- 新增 `StaticPriceFallbackProvider`，作为 `TrainDataProvider` wrapper：
  - 先调用 `LocalStaticPriceProvider` 查询 SQLite 静态票价库。
  - 本地命中时直接返回，不访问远程 provider。
  - 本地未命中或本地数据按 `STATIC_PRICE_MAX_AGE_DAYS` 判定过期时，调用 fallback provider。
  - fallback 返回有价车次时，写回 `od_public_price_snapshot` 和 `od_min_price_snapshot`。
  - fallback 返回空时返回空，不写入静态库。
  - fallback 抛出 `TrainDataProviderError` 或其子类时向上抛出，不吞错误。
- 更新 `backend/app/adapters/provider_factory.py`，支持 `STATIC_PRICE_MODE`：
  - `static-only`：默认模式，只读本地静态库。
  - `local-only`：`static-only` 别名。
  - `static-with-remote-fallback`：本地未命中/过期时远程补洞并写回。
  - `remote-fallback`、`fallback`：补洞模式别名。
- 新增 `STATIC_PRICE_FALLBACK_PROVIDER`，当前支持 `12306-public-price`。
- 更新 `docs/静态票价数据库设计.md`，把 `static-with-remote-fallback` 从设计项回填为已支持能力。

### 环境变量示例

```powershell
$env:TRAIN_DATA_PROVIDER="static-price"
$env:STATIC_PRICE_DB="data/static_prices.sqlite3"
$env:STATIC_PRICE_MODE="static-with-remote-fallback"
$env:STATIC_PRICE_FALLBACK_PROVIDER="12306-public-price"
$env:STATIC_PRICE_MAX_AGE_DAYS="30"
```

### 新增测试

- 新增 `backend/tests/test_static_price_fallback_provider.py`，覆盖：
  - 本地命中时不调用远程。
  - 本地未命中时调用远程并写入静态库。
  - 本地过期时调用远程并写入静态库。
  - 远程返回空时不写入静态库。
  - 远程异常向上抛出。
  - 候选中转站委托给 fallback provider。
- 更新 `backend/tests/test_railway_12306_public_price.py` 中的 provider factory 测试，覆盖：
  - `STATIC_PRICE_MODE=static-with-remote-fallback`。
  - 未知 `STATIC_PRICE_MODE` 报错。
  - 未知 `STATIC_PRICE_FALLBACK_PROVIDER` 报错。

### 当前验证结果

#### Fallback Provider 相关测试

命令：

```powershell
cd backend; python -m pytest tests/test_static_price_fallback_provider.py tests/test_railway_12306_public_price.py -q
```

结果：

- `30 passed`

#### 后端完整测试

命令：

```powershell
cd backend; python -m pytest -q
```

结果：

- `89 passed`
- `1 warning`

警告来源仍为 FastAPI/TestClient 依赖链中 Starlette 提示 `httpx` 用法弃用，不影响当前功能。

### 当前约束

- `static-with-remote-fallback` 会在用户查询缺失或过期 OD 时访问 12306，查询耗时和失败率受外部网络影响。
- 该模式只负责“查询时补洞写回”，不替代批量刷新脚本。
- 历史最低价仍不得作为 A* 硬剪枝 lower bound。
- 当前 fallback provider 只支持 `12306-public-price`。

## 2026-06-26 静态票价库环境变量文档回填

### 已完成成果

- 更新 `README.md`，新增“静态票价库模式”说明。
- 在 README 中补充两种启动方式：
  - `STATIC_PRICE_MODE=static-only`
  - `STATIC_PRICE_MODE=static-with-remote-fallback`
- README 中明确建议 `STATIC_PRICE_DB` 使用绝对路径，避免从项目根目录和 `backend/` 目录启动时解析到不同 SQLite 文件。
- README 中补充手动刷新静态库命令示例。
- 更新 `docs/静态票价数据库设计.md`，把 `STATIC_PRICE_DB` 示例改为绝对路径，并说明相对路径风险。

### 当前约束

- README 中的绝对路径示例是当前本机项目路径，其他环境需要替换为实际仓库路径。
- `static-with-remote-fallback` 仍会访问 12306 公布票价接口，不保证实时有票。
